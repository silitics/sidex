[project]
name = "sidex-py-tests"
version = "0.0.0"
description = "Python integration tests for Sidex code generation."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.5",
    "pytest>=8",
]
