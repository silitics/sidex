[package]
name = "sidex-diagnostics"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
# Required for rendering diagnostics.
ariadne.workspace = true # PRIVATE
# Required for thread-local diagnostic contexts.
scoped-tls.workspace = true # PRIVATE
# Required for locks and other concurrency primitives.
parking_lot.workspace = true # PRIVATE
# Required for wrapping and handling generic errors.
anyhow.workspace = true # PRIVATE
# Required for Sidex IR data structures.
sidex-ir.workspace = true # PUBLIC

[lints]
workspace = true
